# 個股訊號研究驗收紀錄

本文件保存第一輪歷史驗收；第二輪的資料修復、RSI 獨立假說及專業模式見「個股訊號下一輪驗收.md」。下列資料覆蓋與未部署狀態是第一輪當時的紀錄。

日期：2026-09-27。基準提交：`48d2b685220fe507d829df1211654739d9e68a38`；本輪修改尚未提交、推送或更新執行中的部署。

## 成果與限制

- 沿用既有指標與 15 種訊號，產生 1,083 檔股票／ETF 的完整成績單；5／20 日、資料不足項目全部保留。
- 增加同股票、同大盤情境的已成熟歷史對照、季度等權探索區間、固定時間分段與情境增量比較。
- 本輪預先固定「大盤與個股均為上升趨勢」情境，沒有候選同時通過門檻。多數訓練情境只有 7 個季度，未達 8 季門檻；這不代表全部訊號無效。
- 個股行情涵蓋至 2026-09-24；本機 `^TWII` 情境序列只涵蓋 2021-06-23～2026-06-22。缺少同日指數、未成熟及不連續交易日的事件不納入情境研究。
- 籌碼改由單檔與樣本池共用 `sourceDate` 正規化，同來源日重複快照去重，避免把休市日保存日期當成交易日。
- 已完成個股前瞻帳本與命令，協定凍結程式、研究條件及來源摘要。實際登錄 1 份未啟用協定、0 筆試驗、0 筆結果，沒有歷史回填。
- 既有 API、MCP 與體檢成績單元件加入研究資料及限制。「圖表 → 體檢 → 專業」內容深化依使用者指示留待下一輪。

## 驗證

- 91 項 Python 相關回歸測試通過，執行期間封鎖外部網路。
- 23 項體檢前端契約斷言通過；89 支打包腳本語法檢查通過；隔離 HTML 建置通過。
- 一位隔離上下文、同供應商唯讀審查者完成初審與修正後複審。修正缺日定案、程式版本混用、籌碼生命週期、大盤證據、盤中資料界線、顯示權重及快取覆寫等發現後，未留下阻擋事項。審查者只做靜態覆核，測試由主代理執行。
- 凍結程式、行情快照、籌碼快照、成績單與協定摘要逐項一致。
- 原始 `data/market.db` 前後 SHA-256 相同：`e4452ab5e77bbada53b6f3b6ac75eacf44dd8d44ed1ce50dfbe97ded5c146e75`。
- 隔離預覽已產生；瀏覽器工具的安全政策禁止開啟 `file:` 協定，未進行實際畫面、鍵盤、桌面或窄版瀏覽器操作驗收，也未採取替代管道繞過。

## 本機交付

資料夾位於專案根目錄下的 `scratch/個股訊號研究-20260927/交付驗證`。這些大型快照與本機產物不自動加入版本控制。

- `個股訊號成績單.md`：完整 15 種訊號及情境比較。
- `成績單.json`：完整統計、覆蓋、年份差距、集中程度與研究政策。
- `來源快照.sqlite3`、`籌碼快照.json`、`程式快照`：可追溯輸入。
- `個股訊號前瞻.sqlite3`、`執行紀錄.json`、`前瞻狀態.json`：協定與實際狀態。
- `驗收摘要.json`、`回歸測試.txt`、`前端契約測試.txt`：驗證證據。
- `成績單預覽.html`：以實際研究資料及原有成績單元件產生的隔離預覽，沒有外部連線。

初次運算保留於同層 `正式評估` 資料夾，已另加狀態說明，明確標為非交付版本。

## 前瞻批次的使用方式

目前協定未啟用，因此以下命令只回報未啟用狀態，不會新增試驗。未來若另有通過的協定，應使用該次凍結程式與輸出資料夾追蹤，不混用後續修改的工作區程式。

```powershell
$stockPython = (Get-Content -LiteralPath '.\data\stock_python.path' -Raw).Trim()
& $stockPython -B `
  '.\scratch\個股訊號研究-20260927\交付驗證\程式快照\scripts\個股訊號成績單.py' `
  --mode track `
  --output '.\scratch\個股訊號研究-20260927\交付驗證' `
  --db '.\data\market.db' `
  --chips '.\data\chip_history'
```

此命令只使用已存在的本機資料。補抓外部資料、啟動排程及部署均不包含在本次執行中。
