# Stock Terminal — 加股設定精靈 (Setup Wizard) 設計規格（供 review）

> 目標：**加入一支股票時，用最簡化問答（3 個問題）自動把各項指標 / 警報 / 計畫 / 畫線一次設定好**，背後跑一套「專業股市分析 Skill」依個股當下狀態與使用者意圖給出建議值。
> 交付原則：先 MD review → 確認後實作 `wizard_v3.js`（純前端、IIFE、裸 `S`），盡量重用既有端點，不新增 server 端點（除非 review 要求）。
> 撰寫日期 2026-06-14。基底 v3.9。

---

## 0. 為什麼要 Wizard

現況：加股後，使用者要手動分別去 WATCH 設訊號、🔔 設警報、PLAN 填停利停損、✏ 畫支撐壓力——步驟多、新股容易漏設。Wizard 把這些**收斂成一條問答流**，並用分析引擎**預先算好建議值**，使用者只需確認/微調，一鍵套用。

設計哲學：**問得少、算得多、可覆核**。問題壓到 3 題；其餘全部由引擎依個股實況推導，且每個建議都附「為什麼」與可改的數值，不黑箱。

---

## 1. 觸發點（何時跳 Wizard）

對接既有流程，三種進入：

1. **加入自選股時**：自選股 chip 區的 `＋` 加股、或 `🔬 選股` / `🔗 供應鏈` / ETF 報表點股載入後，偵測「此股尚無任何設定」（無 WATCH 訊號、無警報、無計畫）→ 浮出「✨ 用精靈設定 2330？」小提示（可關、可「下次別問」存 localStorage）。
2. **手動**：工具列新增 `🧙 精靈` 鈕（或自選股 chip 右鍵選單「設定精靈」），對目前個股隨時重跑。
3. **批次**（P2 選項）：對整個自選股清單逐檔跑精靈（進階，先不做）。

不打斷既有 `loadSym`：Wizard 是獨立 modal，套用時才寫入各子系統。

---

## 2. 最簡化問答（只有 3 題，皆單選）

用既有 modal 風格（沿用 sc-modal / sb-modal 的黑金樣式）。每題大按鈕、附小字說明。

**Q1. 這檔對你是？**　（決定要設什麼）
- 🔭 **觀察中**（還沒買，想等買點）→ 設觀察訊號 + 買區警報 + 不建倉
- 💰 **已持有**（要管理停利停損）→ 問進場價/股數 + 設停利停損 + 風險警報
- 🎯 **想建倉**（評估要不要買）→ 跑完整體檢 + 給買區/分批建議 + 觀察訊號

**Q2. 你的操作週期？**　（決定指標參數與訊號靈敏度）
- 📅 **長線（季～年）**：以 60 日線/週線、月營收 YoY、估值河流為主；訊號容差大
- 〰️ **波段（數週～數月）**：20/60 日線交叉、KD、布林為主（預設）
- ⚡ **短線（數日）**：突破/量增/RSI 為主；訊號靈敏、停損緊

**Q3. 風險承受？**　（決定停損幅度與部位提示）
- 🛡️ **保守**：停損 ~5%、買區嚴格、過熱即提醒減碼
- ⚖️ **穩健**（預設）：停損 ~8%
- 🔥 **積極**：停損 ~12%、容許追突破

> 預設值：波段 + 穩健，使用者可三題全跳過直接「用建議值」。
> 進場價/股數（僅 Q1=已持有）：第二步用一個輕量表單問，沿用 PLAN 的輸入欄位。
> **市場派預設**：若該股屬 TSMC/台灣 AI 供應鏈核心（用 `supplychain_v3` 的 CHAIN 名單判定），Q2 預設長線、研判文案帶入結構偏多背景（呼應 user thesis）。

---

## 3. 「專業分析 Skill」引擎（問完後跑，全部用既有資料）

Wizard 套用前，先對個股做一次**體檢**，資料全部來自現成模組/端點（不新增 server）：

| 面向 | 來源（既有） | 用途 |
|---|---|---|
| 技術面分數 | `enhance_v3.computeCanonTech`（1Y 日線，已快取） | 多空傾向、訊號挑選 |
| 指標現值 | `S.ind` / `StratLib`（SMA20/60、RSI、KD、MACD、BB、量比、ATR） | 算支撐壓力、停損幅度、靈敏度 |
| 策略歷史勝率 | `Backtest.scanStrategies(S.data.candles)` | **挑出此股歷史表現最好的 2~3 個策略**當觀察訊號 |
| 基本面 | `/fundamental`（月營收 YoY、三率、評分） | 長線體質、買進信心 |
| 估值位階 | `/valuation`（本益比河流百分位） | 「現在貴不貴」、買區寬鬆度 |
| 籌碼 | `/inst-rank` / `_chip_streak`（投信外資連買天數） | 買盤動能佐證 |
| 供應鏈/連動 | `supplychain_v3` 段別 + `overnight_v3` TSM 連動 | 結構觀點、隔日預估 |
| 波動度 | ATR(14) 或 Ann Vol% | **停損/停利幅度按波動自適應**（非固定 %） |

引擎輸出一份**建議組態**（下節），每項都標：建議值 + 一句理由 + 可調整。

### 關鍵演算法
- **支撐/壓力**：近 N 日 swing high/low + 近期密集成交價（可重用 volume_profile 的 POC/VAH/VAL）→ 取最近的上下各 1~2 條 → 自動畫水平線。
- **停損幅度**：`max(風險檔位下限, k×ATR/price)`，例：穩健 = max(8%, 1.5×ATR%)，讓高波動股不被洗、低波動股不放太鬆。
- **停利/買區**：壓力位當停利參考；買區 = 支撐帶 ± 容差（依 Q2 週期調容差）。
- **訊號挑選**：scanStrategies 依 expectancy 排序，取符合 Q2 週期類型的前 2~3 個（長線偏 pullback/估值、短線偏 breakout/量增）。
- **斐波那契**：若近一段有明顯波段，自動以該波段 high-low 畫 Fib（給回檔買點參考）。

---

## 4. 一鍵套用 → 寫入既有子系統

Wizard 最後一頁是**「建議摘要 + 勾選要套用的項目」**（全勾，可取消個別項），按「✅ 套用」後分別寫入：

- **WATCH 訊號** → 寫入 watch 資料結構（沿用 watch_v2 saveWatches → /watch/rules 後端 24h 偵測）。挑選的 2~3 個策略 + 參數。
- **警報**（🔔 後端）→ 加價格警報（跌破支撐/突破壓力）；Q1=想建倉/觀察時加**買區複合警示**（v3.9 composite，如「收盤 ≤ 支撐 且 RSI < 40」）。
- **計畫/持倉**（PLAN）→ Q1=已持有：寫進場價/股數/停利/停損；Q1=想建倉：寫「計畫單」(目標買區/停損/部位建議)。
- **畫線**（✏ drawtools）→ 程式化加 `S.draw[sym]`：支撐/壓力水平線 + （有波段時）Fib；存 localStorage + 雲端 /draw。
- **研判備註** → 一段「精靈體檢結論」（技術×基本×估值×籌碼×供應鏈一句話總結，市場派視角），存到該股筆記/PLAN note。

套用採**冪等**：重跑會更新（標記 `source:'wizard'` 的項目可被覆寫，使用者手動建的不動）。

---

## 5. 模組設計

- 新檔 `wizard_v3.js`，IIFE，對外 `window.wizardOpen(sym, mkt)` / `wizardClose`。狀態用裸 `S`。
- 工具列鈕 `🧙 精靈`（pro_v2 注入，排在 🔬 選股 後）。
- `build_v2.py` 的 `V2_SCRIPTS` 登記 `wizard_v3.js`（排在它依賴的 backtest_v3 / strategy_builder(StratLib) / drawtools / enhance 之後）。
- 加股提示：在 `addWl` 後 dispatch 的流程掛一個輕量 hook（或監聽 `symLoaded`）偵測「無設定」→ 顯示提示條。
- **不新增 server 端點**：體檢全走既有 `/fundamental` `/valuation` `/inst-rank` + 前端 `Backtest`/`StratLib`/`computeCanonTech`。AI 結論若要更口語可選用既有 `/etf-reason` 風格端點（P2 再評估）。

---

## 6. UX 流程圖（文字版）

```
加股 / 點 🧙
  → [體檢中…] 抓 fundamental/valuation/inst-rank + 算 tech/scan/ATR
  → Q1 用途   → Q2 週期   → Q3 風險   (各題可「用建議」跳過)
  → (若已持有) 進場價/股數
  → 建議摘要頁：
       觀察訊號：黃金交叉(20/60)、回測60日線  [可取消]
       警報：跌破 980 / 突破 1080 / 買區複合(收≤980 且 RSI<40)
       計畫：停損 945(-3.6%, 1.5×ATR)、停利 1080
       畫線：支撐 980 / 壓力 1080 / Fib(波段 850-1080)
       結論：技術偏多(72)、估值河流 58 百分位偏中、投信連買 4 天；
             屬 CoWoS 封裝段、TSM 連動強 → 結構偏多，回檔分批
       [✅ 全部套用]  [自選要套用的]  [取消]
  → 寫入 WATCH/警報/PLAN/畫線/備註 → 關閉
```

---

## 7. 分期

- **W1（核心）**：3 題問答 + 體檢引擎 + 建議摘要 + 套用到 WATCH/警報/畫線/計畫。手動 🧙 鈕進入。
- **W2**：加股自動提示、已持有表單、Fib 自動波段、市場派文案、冪等覆寫。
- **W3（選用）**：批次跑整個自選清單、AI 口語結論、把精靈建議存成可分享的「設定模板」。

---

## 8. 待你 review 的決策點

1. **問題數**：3 題（用途/週期/風險）夠嗎？要不要再加「資金規模」以算建議股數？或反而想再砍成 2 題？
2. **套用範圍**：預設「全套用」可取消個別項；還是預設「只建議不寫入、逐項手動確認」？
3. **停損邏輯**：接受「風險檔位 + ATR 自適應」取大值嗎？還是要純固定 %（5/8/12）？
4. **買區複合警示**：要不要預設幫「觀察/想建倉」的股自動建一條 composite 買區警示（會用到後端 daemon，需開啟推播）？
5. **AI 結論**：體檢結論要純規則生成（免 API、即時），還是要選配 Claude 口語版（需 API KEY、較慢）？
6. **觸發提示**：加股自動跳提示會不會太吵？要不要預設關、只留工具列 🧙 鈕手動開？
