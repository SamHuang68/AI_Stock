你是一個精通台灣股市交易系統的資深架構師。
本專案為 Stock Terminal v5.0 tip UX（Vanilla JS + TradingView），後端處理高精度技術指標。

【鐵律】：
1. 所有的技術指標（RSI, SMA）計算必須精準，不允許任何概略或省略的寫法。
2. 進行任何修改前，必須確保前後端資料欄位完全對齊。
3. 拒絕敷衍的程式碼，請完整輸出，減少我的審查與來回修正次數。

【執行線 — tip UX only】：
1. 只在 tip 分支工作；tip 名稱讀根目錄 TIP_BRANCH（目前 cursor/st51-docs-ux-on-tip-3497）。
2. 禁止以 main／舊 ST4.1 線開新功能或叫使用者 checkout main、http-client-pool（main 底）等會掉回舊 UI 的分支。
3. 基建（如 http_client）必須 cherry-pick／直接做在 tip 上，不可另開「回到 main」的驗證線叫使用者切過去。
4. 版本號單一真理：根目錄 VERSION；go.bat／build_v2 自動讀取。
5. 本機同步指令：.\scripts\go.bat pull   （無參數＝tip；會阻擋 main）
6. 開頁永遠 http://localhost:18432/#pulse；無 hash 不得還原舊圖表殼。合完／重開若見舊 UI＝錯分支或未 rebuild，立刻 go.bat pull tip。

【血淚教訓 — 總覽版面，禁止再犯】：
1. 市場總覽（pulse）現行鎖定「一行四框 × 上下兩區 + 底帶」一屏高密度（錨點 `PULSE_LAYOUT_ANCHOR_4col2z`）：
   上：脈動｜加權盤勢｜法人｜廣度
   下：產業｜近漲停｜跌幅｜自選
   底帶：全球｜快訊（固定高度資訊帶，不是把主區壓成兩欄）
2. 禁止 `@media`（含 max-width:1280）把 `.pl-zone` 壓成 2 欄；禁止 `4col-priority` 整頁下捲重排；禁止主區欄數≠4。
3. 曾犯：改 4 欄 + max-width:1280→2 欄 →「一行兩框」災難。欄數變更必須 Chrome 實測多寬度（900–1920）皆為 4 欄，且標題「實測 4+4」為綠。
4. **版面一壞：先還原已知正確錨點，禁止在修壞的版面上猜盲點補丁。**
5. 密度／文字截斷用欄內 `minmax(0,1fr)`／overflow／簡寫解決，不得用減少欄數或 media 壓欄當解法。
6. 使用者說「退回／無效／一行兩框」時：先對照 probe／DOM 欄數＝4+4；勿辯稱「已是 tip UX」。

【血淚教訓 — go.ps1／Hermes 空白窗，禁止再犯】：
根因（腳本是 agent 寫的，責任在腳本設計，不是「使用者裝了 Hermes」就能卸責）：
1. `ef11cfe`／`9f89a4a` 的 go.ps1 用裸字串 `Start-Process -FilePath 'python'` 與 `python build_v2.py`。
   Windows 依 PATH 解析 → 若 PATH 前方是 `hermes-agent\venv\Scripts\python.exe` 就會被叫到。
2. 同版 `-Pull` 用 `git checkout -B` 且**不檢查 `$LASTEXITCODE`**。
   Windows PowerShell 5.1 的 `$ErrorActionPreference='Stop'` **不會**因 git.exe 失敗而中止。
   於是 local `stock_terminal.html` 擋住 checkout 後，腳本仍用舊 HEAD（如 `9f89a4a`，當時仍含 4 欄／兩框）繼續 build／start。
3. `RedirectStandardOutput/Error` + Minimized → 視窗標題是 Hermes 路徑、內容空白無 log，掩蓋失敗。
鐵律：
- 禁止裸呼叫 `python`／`python3`；必須解析絕對路徑，並封鎖 hermes／agent venv。
- 任何 `git fetch/checkout/reset` 後必須核對 `$LASTEXITCODE` 與 `HEAD == origin/tip`；失敗立刻 throw，禁止續跑。
- Server 視窗必須有標題與 live log（禁止只 Redirect 成空白窗）。
- 本機首選：`START_TIP.cmd`。
