# Stock Terminal v3.9 — 外部建議書「實用性」評估與階段規劃

> 以專業分析師角度，對外部 AI 產出的改進建議書逐項判讀，再給出**符合本專案實況**的階段規劃。
> 評估日期：2026-06-16

---

## 一、先校正三個錯誤前提

外部建議書是**通用範本**，它的技術選型與路線圖建立在三個與本專案不符的假設上：

| 範本假設 | 本專案實況 | 影響 |
|---|---|---|
| .NET / C# 技術棧（SignalR、MEF、ASP.NET Core、IdentityServer4、Xamarin/MAUI） | **Python `http.server` 自訂 handler + 原生 JS IIFE 模組**（`<script src>` 各自註冊 `window.xxx`，`build_v2.py` 注入） | 範本一半的「技術選型」直接不適用 |
| 多用戶雲端 SaaS（user_id 表、OAuth2/JWT、多客戶端同步、限流防濫用） | **單人本機工具**（localhost:18432，只有 Sam 一人用） | 認證/多租戶/同步全是多餘的攻擊面與工程負擔 |
| 從零開始的綠地專案 | **v3.9，已有數十個模組**（見下表） | 範本的「核心功能」大多**早已實作** |

**結論：約 60% 已實作、25% 不適用、15% 值得做。** 而範本**完全沒提到**本專案真正的瓶頸——**資料源可靠性**（這才是近期所有 bug 的根源）。

---

## 二、逐項判讀

圖例：✅ 已實作　⚠️ 部分已有/可強化　👍 值得做（範本沒講或方向需修正）　❌ 不適用

### 功能層面

| # | 範本建議 | 判讀 | 說明 |
|---|---|---|---|
| 1 | 多市場支援 | ⚠️ | 已有台股(.TW/.TWO)+美股+四大指數(^TWII/^DJI/^GSPC/^IXIC/^SOX)。港股可加但 Sam 聚焦台股/美AI供應鏈，**低優先** |
| 2 | 即時行情 WebSocket | ❌ | **台股無公開免費 WS**（TWSE 不提供）；Yahoo 也只能輪詢；美股 WS 需付費(IEX/Polygon)。現行**輪詢+快取已足**，個人工具不值得 |
| 3 | 技術指標套件 | ⚠️ | 已有 volume_profile/pattern/peg/valuation。MA/EMA/RSI/MACD/BB 是否齊全+參數可調**需確認**；用 JS 自建即可，**不需** TA-Lib C# wrapper |
| 4 | 自選清單+分組 | ⚠️ | 自選股已有(S.wl)。「長線/短線」分組可加，**localStorage tag 即可**，不需 user_id DB 表 |
| 5 | 模擬交易 Paper Trading | ⚠️ | 已有 plan_position/plan_history(計畫/持倉)。盈虧計算可強化 |
| 6 | 基本面整合 | ✅ | fundamental_v3 / peg_v3 / valuation_v3（公開資訊觀測站/Yahoo） |
| 7 | 事件提醒(新聞/法人) | ✅ | alert_v3 / alert_push_v3(Telegram/Email) / calendar_v3 / chip_v3 / instrank_v3 |
| 8 | 策略回測引擎 | ✅ | backtest_v3 / backtest_ui_v3 / strategy_builder_v3 / strategy_script_v3 |
| 9 | 多語系 i18n | ❌ | 單用戶、繁中，不需要 |
| 10 | RESTful API + JWT | ❌ | 已是本機 server，內部 endpoint 齊備(/quote /stockfut /screen3 /macro…)。**JWT/OAuth 是多用戶雲端概念**，單機只會增加風險面 |

### 使用者體驗

| # | 範本建議 | 判讀 | 說明 |
|---|---|---|---|
| 1 | 響應式+暗色模式 | ✅ | Bloomberg 深色主題 + mobile_v2.css |
| 2 | 可拖拉儀表板 Widget | 👍(低) | multichart_v3 已部分。完整拖拉版面工程大、個人固定版面已夠，**觀望** |
| 3 | 快捷鍵 + Command Palette | ⚠️ | hotkeys_v3 已有快捷鍵；命令面板可加，**中低優先** |
| 4 | 圖表交互(畫線/註解) | ✅ | drawtools_v3 |
| 5 | 多螢幕同步 | ❌ | 單機單人不需要(SignalR 多客戶端概念) |
| 6 | 警示音+桌面通知 | ⚠️ | 已有推播(Telegram/Email)；**桌面 toast 可補**，輕量 |

### 效能 / 安全性

| # | 範本建議 | 判讀 | 說明 |
|---|---|---|---|
| 1 | 緩存層(Redis) | ⚠️ | server.py 已有 `_cache` LRU + `_pool` 執行緒池。**Redis 對單用戶過度**，維持現行 |
| 2 | 限流 Rate Limiting | 👍 | **方向需修正**：不是「擋使用者」，而是「**對外源節流+退避+快取**」避免被源端 ban（TAIFEX 已實際遇 520）。stockfut 已做局部(45s cache/批次/retry)，**值得系統化** |
| 3 | OAuth2/JWT 認證 | ❌ | 單機個人工具不需要 |
| 4 | 數據完整性與備份 | 👍 | chip/etf JSON 是核心資產，**每日快照/版本備份輕量值得** |
| 5 | Prometheus+Grafana | ❌ | 過度。簡單 log + **源健康檢查面板**即可 |

### 擴充性 / 維護

| # | 範本建議 | 判讀 | 說明 |
|---|---|---|---|
| 1 | Plugin 架構(MEF) | ✅ | **其實已是**：IIFE 模組各自註冊 `window.xxx`、build_v2 動態注入。已模組化，不需 .NET MEF |
| 2 | CI/CD | ❌/👍(輕) | 個人專案，build_v2 / build_dist.bat 已是打包流程。GitHub Actions 過度，**可選** |
| 3 | 單元/整合測試 | 👍 | **真正值得**：我們在 parseYF/昨收口徑/MIS 解析上**重複踩同類 bug**，核心資料解析該有迴歸測試 |
| 4 | Swagger/OpenAPI | ❌ | 內部 endpoint、單人，不需要 |
| 5 | 多平台(Electron/MAUI) | ❌ | 已是瀏覽器存取的本機 web app，**任何瀏覽器即跨平台**，不需 Electron/MAUI |

---

## 三、範本漏掉的真正痛點：資料源可靠性

近期所有災情——`00988A 漲幅亂跳`、`櫃買亂跳`、`費半量柱不顯示`、`加權 6/15-16 缺口`、`TAIFEX 520`、`切股 race`——**全是資料層問題，不是缺功能**。範本一個字都沒提。對 Sam 最在乎的 **Data Integrity**，這才是 ROI 最高的投資方向。

已知壞點：
- **^TWOII(櫃買)**：Yahoo 三端點三值(chart 419 / quote 269 / live 105)，已停用、改走 TWSE MIS。
- **Yahoo 昨收口徑**：`chartPreviousClose` 在不同 range 意義不同；null 日線缺口會讓 `candles[-2]` 取到更早的日期 → 假漲幅。
- **TAIFEX MIS**：20 路並發即 520，需節流+退避。

---

## 四、務實階段規劃（取代範本的 A–E 路線圖）

### Phase 0 — 資料可靠性地基（最高優先，~1 週）
這是真痛點，先做。
- **多源備援框架**：對 Yahoo 壞標的（^TWOII 等）以 TWSE/TPEx MIS 為主源；建立 source priority + 自動 fallback。
- **統一昨收口徑 + null-gap 處理**：把已修的散點(loadSym /quote fallback、/quote-batch)收斂成單一可信函式。
- **對外源節流/退避/快取**：把 stockfut 的 ad-hoc 做法系統化成共用層，杜絕 520。
- **資料健檢面板**：每個來源「最後成功時間 / 異常標記」一眼可見。

### Phase 1 — 鞏固既有半成品（~2–4 週）
- 自選股**分組 tag**（長線/短線，localStorage）。
- **指標補齊確認**（MA/EMA/RSI/MACD/BB 參數可調）。
- **桌面 toast 通知**（補足現有 Telegram/Email 推播）。
- paper trading **盈虧強化**（plan_position）。

### Phase 2 — 測試與穩定
- 對**核心資料解析寫單元測試**（parseYF / 昨收 / stockfut MIS / 指標計算）——防迴歸。
- **每日資料快照備份**（chip/etf JSON 版本化）。

### Phase 3 — 選配（低優先 / 觀望）
- Command Palette、可拖拉版面（nice-to-have）。
- 港股（視需求）。

### 明確「不做」清單（對單機個人 Python+JS 工具屬過度或不適用）
WebSocket（台股無源）、OAuth/JWT、多用戶 DB、Redis、Prometheus/Grafana、Swagger、Electron/MAUI、i18n、多螢幕同步、.NET MEF plugin。

---

## 一句話總結
範本把你當成「要從零打造的 .NET 多用戶 SaaS」——但你已經有一個成熟的單機 Python+JS 終端機。**別照它的路線圖做新功能；把資源投到資料源可靠性（Phase 0）**，那才是你近期所有 bug 的根，也最符合你的 Data Integrity 原則。
